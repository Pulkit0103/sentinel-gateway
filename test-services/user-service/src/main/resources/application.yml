server:
  port: ${USER_SERVICE_PORT:8082}
spring:
  application:
    name: user-service
management:
  endpoints:
    web:
      exposure:
        include: health,info
  info:
    env:
      enabled: true
info:
  app:
    name: User Service
    description: Phase 2 test downstream service
